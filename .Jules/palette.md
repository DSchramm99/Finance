## 2025-05-15 - [Enhanced Data Clarity with column_config]
**Learning:** Using `st.column_config.ProgressColumn` for bounded metrics (0-100) and `help` tooltips in `st.dataframe` significantly improves the interpretability of technical financial data without cluttering the UI.
**Action:** Always prefer `ProgressColumn` for scores and provide `help` descriptions for technical jargon in DataFrames.
